name: mathpuzzle_app

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  http: ^1.2.1 # For API requests
  provider: ^6.1.2 # For state management
  socket_io_client: ^3.1.4 # For WebSocket
  shared_preferences: ^2.2.3 # For local storage

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
